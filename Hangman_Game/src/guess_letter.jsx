import pick_word from './pick_word'

const checkGuessedLetter = (guessedLetter) => {
    return document.getElementById.innerText = game_word.map((char) => {  
       return char === guessedLetter ? char : "_"; }); };